package AST.VALDECLnode;

import AST.ASTnode;
import Utils.position;

abstract public class Declare_ASTnode extends ASTnode {
    public Declare_ASTnode(position pos) {
        super(pos);
    }
}
